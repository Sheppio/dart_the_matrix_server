name: dart_the_matrix_server
description: A server app using the shelf package and Docker.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: ">=2.18.4 <3.0.0"

dependencies:
  args: ^2.0.0
  color: ^3.0.0
  #image: ^3.2.2
  riverpod: ^2.1.1
  shelf: ^1.1.0
  shelf_router: ^1.0.0
  shelf_web_socket:
  web_socket_channel:

dev_dependencies:
  http: ^0.13.0
  lints: ^2.0.0
  test: ^1.15.0
